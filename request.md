
## 项目提示词：校园互助平台（轻量级）

### 1. 项目概述
开发一个专属于高校师生的轻量级互助平台，解决零散、临时、非标准化需求的发布与对接。核心场景包括：跑腿、学业搭档、招募、生活交易等。系统必须提供**任务生命周期管理**、**任务绑定私信**、**简单信用评价**、**校内身份验证机制**。

### 2. 技术栈（严格遵循）
- **前端**：Vue 3 (Composition API) + Vite + Pinia（状态管理）+ Vue Router + Axios + Socket.io-client
- **后端**：Node.js + Express + Socket.io + JWT（身份认证）
- **数据库**：MySQL 8.0 + 使用连接池
- **实时通信**：Socket.io（用于任务状态变更通知、私信即时提醒）
- **其他工具**：bcrypt（密码加密）、multer（可选文件上传，如任务配图）、dotenv

### 3. 核心功能需求（MVP 必须包含）

#### 3.1 用户模块
- 注册/登录（使用学号/工号 + 校园邮箱验证，或简单手机号验证，MVP 阶段可先做邮箱验证码）
- 个人资料：昵称、头像（可选）、身份（学生/教师）、信用分（初始 100）
- JWT 鉴权，前端路由守卫

#### 3.2 任务模块
- **发布任务**：标题、描述、分类（跑腿/学业/招募/生活）、赏金（积分或真实货币？MVP 可做虚拟积分或免费，但保留字段）、截止时间、地点（可选）
- **任务列表**：分页、按分类筛选、按发布时间排序、可搜索关键词
- **任务详情**：显示完整信息、发布者信息、当前状态（进行中/已完成/已取消）
- **接单/完成流程**：
  - 任务发布后为“招募中”
  - 其他用户可“接单” → 状态变“进行中”，记录接单者
  - 发布者可“确认完成” → 状态变“已完成”，双方信用分变化（接单者 +1，发布者 +0.5 防刷）
  - 发布者或接单者可“取消任务”（仅限进行中之前或双方协商）
- 任务评论/留言？MVP 阶段不单独做，依靠私信模块

#### 3.3 私信模块（与任务绑定）
- 每个任务自动生成一个**会话（session）**，任何查看任务详情的人可向发布者或接单者发送消息（但需遵循任务状态）
- 私信独立于任务列表，但消息必须关联任务 ID，方便追溯
- 使用 Socket.io 实现实时消息推送，同时存储到 MySQL
- 前端聊天窗口：显示历史消息 + 实时收发

#### 3.4 评价与信用分
- 任务完成后，双方可互评（1-5 星），评价影响信用分
- 每个用户资料页显示信用分和完成的任务数

#### 3.5 身份验证基本防护
- 注册时必须填写学校邮箱后缀（如 `@xxx.edu.cn`）或通过邀请码（MVP 可简单正则校验）
- 后端对所有请求做 JWT 校验（除注册/登录）

### 4. 开发方式：Agent Team 协作模式

请模拟一个 **4 人敏捷团队**，每位 Agent 有明确角色与职责，并通过「共享上下文」和「迭代评审」协作。

| 角色 | 职责 | 产出物 |
|------|------|--------|
| **后端 & 数据库负责人** | 设计数据库 ER 图，编写 Express 路由、控制器、中间件，实现 API 与 Socket.io 服务 | SQL 建表脚本、Node.js 服务代码、API 文档（Swagger 或 Markdown） |
| **前端负责人** | 开发 Vue3 组件、路由、Pinia store，对接后端 API 与 WebSocket | 完整前端工程目录、可交互页面 |
| **实时通信负责人** | 专门实现 Socket.io 连接管理、房间（按任务 ID 或用户 ID 划分）、消息存储逻辑，与后端负责人配合 | socket 服务端与客户端集成代码、消息模型 |
| **集成与测试负责人** | 编写关键流程的集成测试（如任务发布→接单→完成→评价），确保前后端联调无误，部署简单说明 | 测试用例、Postman 集合、docker-compose（可选） |

**协作流程**：
1. **Sprint 规划**：所有 Agent 一起确认各模块接口契约（API 请求/响应格式，Socket 事件名）。
2. **并行开发**：后端先完成 API 骨架与数据库；前端使用 Mock 数据或等待真实 API；实时通信负责人同时开发 socket 模块。
3. **每日同步**：Claude Code 输出每个角色的进度报告，并解决阻塞问题。
4. **集成演示**：每完成一个用户故事（如“发布任务并被人接单”），集成测试负责人进行端到端验证。

### 5. 详细开发任务清单（按优先级排序）

#### Phase 0: 项目初始化
- 创建项目目录：`backend/`, `frontend/`, `database/`
- 初始化 npm，安装依赖（前后端）
- 配置环境变量（`.env`）：数据库连接、JWT 密钥、端口
- 创建基础文件结构（如 Express app.js, Vue main.js）

#### Phase 1: 数据库设计与连接
- 设计表：`users`, `tasks`, `messages`, `reviews`
- 编写 SQL 建表脚本（含索引：如 task.status, user.school_email）
- 后端实现数据库连接池，测试连接

#### Phase 2: 用户认证 API
- `POST /api/auth/register`（校验邮箱后缀）
- `POST /api/auth/login`（返回 JWT）
- `GET /api/auth/profile`（需 token）
- 前端：注册/登录页面，存储 token，路由守卫

#### Phase 3: 任务 CRUD API
- `POST /api/tasks`（发布）
- `GET /api/tasks`（列表 + 分页 + 筛选）
- `GET /api/tasks/:id`（详情）
- `PUT /api/tasks/:id`（更新状态：接单、取消、完成）
- 权限控制：只有发布者可完成/取消；只有未接单时可被接单
- 前端：任务发布表单、任务列表页、任务详情页

#### Phase 4: Socket.io 私信模块
- 服务端：用户连接时验证 JWT，加入自己的 room（`user_${userId}`）
- 发送消息事件：`send_message`，包含 `{ taskId, toUserId, content }`
- 存储消息到 `messages` 表
- 广播消息到接收者的 room
- 前端：在任务详情页下方展示聊天界面，监听 `new_message` 事件，滚动显示

#### Phase 5: 评价与信用分
- `POST /api/reviews`（创建评价，关联 taskId 和 reviewerId/revieweeId）
- 更新用户信用分（触发数据库事务）
- 前端：任务完成后弹出评价模态框，用户资料页显示信用分

#### Phase 6: 集成测试与优化
- 测试负责人编写关键场景脚本（例如：用户A发布任务 → 用户B接单 → A完成 → B评价）
- 修复边缘情况（重复接单、已完成后禁止消息等）
- 增加简单 UI 提示（toast 通知）

#### Phase 7: 部署准备（可选）
- 编写 README.md：如何初始化数据库、运行后端、前端
- 提供 `docker-compose.yml`（MySQL + Node + Nginx 静态）

### 6. 非功能性要求
- **安全性**：防止 SQL 注入（使用参数化查询或 ORM 如 Prisma/Knex），JWT 过期时间 7 天，敏感数据（密码）bcrypt 加密存储。
- **实时性**：私信延迟 < 500ms。
- **代码风格**：ESLint + Prettier（Airbnb 规则简化版），前端组件使用 `<script setup>`。
- **文档**：每个 API 路由需有 JSDoc 注释；前端复杂组件提供简述。

### 7. 交付物清单
当全部任务完成后，请提供：
1. 完整的代码仓库（或文件树）
2. 数据库初始化 SQL 文件
3. 启动项目步骤（包括 Node 版本要求）
4. 一个简短的演示视频脚本（或关键截图说明）

### 8. Claude Code 工作方式约束
- **不要一次性生成全部文件**，按 Phase 顺序逐个提供可运行的代码块。
- 每完成一个 Phase 后，**主动询问**：“Phase X 已完成，请确认是否继续 Phase Y”。
- 对于涉及前后端协议的部分（如 API 响应格式），请先定义 **TypeScript 接口**（或 Joi 验证模式）再实现代码。
- 如果发现设计缺陷（如遗漏任务标签），请及时指出并提议修改。

---

**开始开发**  
请先按照 Phase 0 初始化项目结构，并输出目录布局和关键配置（`.env` 示例、`package.json` 依赖列表）。等待用户确认后，再进入 Phase 1。